# Preserve raw benchmark evidence byte-for-byte without treating simulator log
# spacing or CSV CRLF records as source-code whitespace errors.
results/** -whitespace
docs/pstmo_bao_cao_toan_dien_assets/*.csv -whitespace
